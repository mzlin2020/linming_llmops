# Phase 1 运行时依赖：web / db / auth 栈。
# 重型 AI 依赖（langchain*/langgraph/qdrant/sentence-transformers/torch/transformers/
# pypdf/jieba/tiktoken 等）在 Phase 3 移植核心引擎时加入。
flask>=3.1,<4
flask-sqlalchemy>=3.1,<4
flask-migrate>=4.1,<5
flask-login>=0.6,<1
flask-cors>=5.0,<6
gunicorn>=23.0

SQLAlchemy>=2.0,<3
alembic>=1.14,<2
pymysql>=1.1
cryptography>=43

pydantic>=2.10,<3
injector>=0.22
PyJWT>=2.10

redis>=5.2
celery>=5.4
concurrent-log-handler>=0.9

python-dotenv>=1.0
PyYAML>=6.0
